class_name ModifierTypes

## ModifierTypes: Enums for the tile modifier system.

enum Type { NONE, EXTRA, MULTI, RESET, EXPO, LOCKED }
enum Lifetime { CONSUMABLE, PER_ROUND, PERMANENT }
enum Tier { BRONZE, SILVER, GOLD }
